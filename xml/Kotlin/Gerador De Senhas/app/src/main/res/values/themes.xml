<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GeradorDeSenhas" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/laranja</item>
        <item name="android:statusBarColor">@color/laranja</item>
        <item name="colorSecondary">@color/laranja</item>
        <item name="colorOnSecondary">@color/white</item>
    </style>

    <style name="Theme.GeradorDeSenhas" parent="Base.Theme.GeradorDeSenhas" />
</resources>