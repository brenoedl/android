<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/barMain"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/yellow"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:title="@string/app_name"
        app:titleTextColor="@color/white" />

    <EditText
        android:id="@+id/blocoNotas"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text=""
        android:hint="@string/rint"
        android:textColor="@color/black"
        android:background="@color/white"
        android:textSize="18sp"
        android:gravity="start"
        android:inputType="textMultiLine"
        android:singleLine="false"
        android:padding="15dp"
        app:layout_constraintTop_toBottomOf="@id/barMain"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginTop="55dp"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btnSalvar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:backgroundTint="@color/yellow"
        android:clickable="true"
        app:layout_constraintBottom_toBottomOf="@id/blocoNotas"
        app:layout_constraintEnd_toEndOf="@id/blocoNotas"
        app:layout_constraintHorizontal_bias="0.944"
        app:layout_constraintStart_toStartOf="@id/blocoNotas"
        app:layout_constraintTop_toTopOf="@id/blocoNotas"
        app:layout_constraintVertical_bias="0.868"
        app:rippleColor="@color/white"
        app:srcCompat="@drawable/ic_check"
        tools:ignore="VectorDrawableCompat" />
</androidx.constraintlayout.widget.ConstraintLayout>